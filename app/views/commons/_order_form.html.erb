<li class='white'>
  <div class="order_form">
    <h1>ОФОРМЛЕНИЕ ЗАКАЗА</h1>
    <div class="left">
      <div class="new_price">
        Всего <strong><%= t('order.price') %></strong><span>руб.</span>
      </div>
      <div class="old_price">
        вместо 1690<span>руб.</span>
      </div>
    </div>

    <div class="new_left">
      <div>
        <%= select_tag "city", options_for_select([ "Томск", "Другой город"  ], "Томск"), class: 'input js-select-city select' %>
      </div>
    </div>

    <div class='right'>
      <%= simple_form_for @order, remote: true do |f| %>
        <%= f.input :first_name, input_html: { class: 'input', placeholder: 'Имя' }, label: false %>
        <%= f.input :phone, input_html: { class: 'input', placeholder: 'Телефон' }, label: false%>
        <%= f.input :email, input_html: { class: 'input', placeholder: 'E-mail' }, label: false %>
        <%= f.submit 'Заказать', class: 'button' %>
        <div class="count">
          <h3>Количество</h3>
          <div class='minus'>
            -
          </div>
          <div class="number">
            <%= f.input :quantity, label: false %>
          </div>
          <div class="plus">
            +
          </div>
        </div>
        <!-- <a href='#' class='button js-open-form js-send-form'>отправить заказ</a> -->
        <div class="prepayment">
          100% предоплата
        </div>
      <% end %>
    </div>

    <div class="new_right">
      <div class="annotation">
        Забрать свой заказ самостоятельно Вы можете по адресу:
        <a href='#map'><%= t('order.address') %></a>
        <span class='or'>
          Либо
        </span>
      </div>

      <div>
        <%= text_field_tag :address, '', class: 'input', placeholder: 'Введите адрес доставки'%>
      </div>
    </div>
    <div style='clear: both'></div>
  </div>
</li>
